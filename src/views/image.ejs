<!DOCTYPE html>

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title><%= title %> | <%= image.name%></title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
  </head>

  <body style="height: 100vh">
    <!-- render image with specified width and height -->
    <% if(!invalidHeight && !invalidWidth && imageExist ) {%>
    <div class="container h-100 p-3">
      <div
        class="row justify-content-md-center align-content-md-center bg-danger rounded text-white h-20"
      >
        <div class="col-md-auto"><h3>You choosed - <%= image.name%></h3></div>
      </div>
      <div class="p-3 col-md">
        <img
          class="rounded"
          style="cursor: pointer"
          src="/thumbnails/<%= image.name%>-<%= width%>-<%= height%><%= image.ext%>"
        />
      </div>
    </div>

    <!-- handle invalid filename -->
    <%}else if(!imageExist){%>
    <div class="container h-100 p-3">
      <div
        class="row justify-content-md-center align-content-md-center bg-danger rounded text-white h-20"
      >
        <div class="col-md-auto"><h3>Bad Inputs</h3></div>
      </div>
      <div class="p-3">
        <p>
          Please enter valid filename that exist or choose from the
          <a href="/">list </a>.
        </p>
      </div>
    </div>
    <!-- handle invalid width and height -->
    <%}else if(invalidHeight && invalidWidth){%>
    <div class="container h-100 p-3">
      <div
        class="row justify-content-md-center align-content-md-center bg-danger rounded text-white h-20"
      >
        <div class="col-md-auto"><h3>Bad Inputs</h3></div>
      </div>
      <div class="p-3">
        <p>You entered invalid width and height <a href="/">try again</a>.</p>
      </div>
    </div>
    <!-- handle invalid width -->
    <%}else if(invalidWidth){%>
    <div class="container h-100 p-3">
      <div
        class="row justify-content-md-center align-content-md-center bg-danger rounded text-white h-20"
      >
        <div class="col-md-auto"><h3>Bad Inputs</h3></div>
      </div>
      <div class="p-3">
        <p>You entered invalid width <a href="/">try again</a>.</p>
      </div>
    </div>
    <!-- handle invalid height -->
    <%}else if(invalidHeight){%>
    <div class="container h-100 p-3">
      <div
        class="row justify-content-md-center align-content-md-center bg-danger rounded text-white h-20"
      >
        <div class="col-md-auto"><h3>Bad Inputs</h3></div>
      </div>
      <div class="p-3">
        <p>You entered invalid height <a href="/">try again</a>.</p>
      </div>
    </div>
    <%}%>
  </body>
</html>
